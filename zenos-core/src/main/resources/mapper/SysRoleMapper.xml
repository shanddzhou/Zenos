<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.zhoushan.blog.persistence.mapper.SysRoleMapper">
    <resultMap id="rm" type="com.zhoushan.blog.persistence.beans.SysRole">
        <id property="id" column="id" jdbcType="BIGINT"/>
        <result property="name" column="name" jdbcType="VARCHAR"/>
        <result property="available" column="available" jdbcType="BIT"/>
        <result property="description" column="description" jdbcType="VARCHAR"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
    </resultMap>
    <select id="findPageBreakByCondition" resultMap="rm" parameterType="com.zhoushan.blog.business.vo.RoleConditionVO">
        SELECT
        com.*
        FROM
        sys_role com
        WHERE
        1=1
        <if test="keywords!=null and keywords!=''">
            AND(
            com.description LIKE CONCAT('%',#{keywords,jdbcType=VARCHAR},'%')
            );

        </if>
        ORDER BY
        com.create_time DESC
    </select>
    <select id="queryRoleListWithSelected" resultMap="rm" parameterType="Integer">
        SELECT r.id,
               r.name,
               r.description
                   (
                       CASE
                           WHEN (
                               SELECT ur.role_id
                               FROM sys_user_role ur
                               WHERE ur.user_id = #{userId}
                                 AND ur.role_id = r.id
                           ) THEN
                               1
                           ELSE
                               0
                           END
                   ) AS selected
        FROM sys_role r
        WHERE r.available = 1
    </select>
    <select id="listRolesByUserId" resultMap="rm" parameterType="Long">
        SELECT r.id,
               r.name,
               r.description
        FROM sys_role r
                 INNER join sys_user_role ur ON ur.role_id = r.id
        WHERE ur.user_id = #{userId}
          AND r.available = 1
    </select>


</mapper>